export function General() {
  return (
    <section>
      <h1>General</h1>
      <ul>
        <li>
          You can only use hooks inside function components, not Class
          components
        </li>
        <li>
          Every time your function runs, the hooks must execute in the exact
          same order
        </li>
        <li></li>
      </ul>
    </section>
  );
}
